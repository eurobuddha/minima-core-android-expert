<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Used only to download the optional on-device LLM on first "Enable AI"
         (Phase 1); everything else — corpus search + embeddings — is fully offline. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- allowBackup=false is a FUND-SAFETY setting here, not a preference. With the scaffold default
         (true + empty rules) Android Auto Backup uploaded this app's whole data directory to the user's
         cloud and RESTORED it on reinstall — which both exfiltrates the data below and re-plants state a
         reinstall is supposed to clear. A reinstall must be a true reset.
         At stake: minima-expert-ai → "key": a live, billable third-party API key held in cleartext. Auto Backup was
         uploading it to the user's cloud and re-planting it on any restored device -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Utxo"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SetupActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>
